spring:
  application:
    name: address-service

  datasource:
    url: jdbc:mysql://localhost:3306/peopleinfo
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true


server:
  port: 8084
  servlet:
    context-path: /address


